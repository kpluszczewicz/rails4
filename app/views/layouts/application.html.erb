<!DOCTYPE html>
<html>
<head>
  <title>Rails4</title>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  </header>
    <h1>Monkey Presents</h1>
  </header>
  <nav>
    <ul>
      <li><%= link_to "Home", welcome_path %></li>
      <li><%= link_to "Listing presentation", presentations_path %></li>
        <% if user_signed_in? %>
          <li><%= link_to "Profile (#{current_user.email})", profile_path %></li>
          <li><%= link_to "New presentation", new_presentation_path %></li>
          <li><%= link_to "Sign out", destroy_user_session_path %></li>
        <% else %>
          <li><%= link_to "Sign in", new_user_session_path %></li>
          <li><%= link_to "Sign up", new_user_registration_path %></li>
        <% end %>
      <li><%= link_to "About us", aboutus_path %></li>
    </ul>
  </nav>

  <section>
  <% flash.each do |key, value| -%>
    <div class="flash <%= key %>"><%= value %></div>
  <% end -%>

  <%= yield %>
  </section>

  <footer>
    <p>Copyright 2011</p>
  </footer>

  <%= debug(params) if Rails.env.development?  %>
</body>
</html>
